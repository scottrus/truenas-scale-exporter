name: CI

on:
  pull_request:
  push:
    branches: [main]

# A force-push or a rapid second push should cancel the older run rather than
# queue behind it.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

# Every job below runs a `make` target that a contributor can run locally.
# REQUIRE_ALL=1 makes a missing tool a hard failure, so CI can never silently
# skip a check that only appears to have run.
env:
  REQUIRE_ALL: "1"

jobs:
  python:
    name: Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        # 3.11 is the floor pyproject claims, so it catches an accidental
        # newer-syntax dependency. 3.14 is what the published image actually
        # runs — the Chainguard base tracks the newest Python, so without it
        # the one interpreter users receive would be the one nothing tests.
        python-version: ["3.11", "3.13", "3.14"]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Lint (ruff, ruff format, pylint)
        run: make lint

      - name: Tests
        run: make test

  workflows:
    name: Workflows
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install actionlint
        run: |
          set -euo pipefail
          curl -sSL -o actionlint.tar.gz \
            https://github.com/rhysd/actionlint/releases/download/v1.7.12/actionlint_1.7.12_linux_amd64.tar.gz
          tar xzf actionlint.tar.gz actionlint
          sudo install -m 0755 actionlint /usr/local/bin/actionlint

      - name: Lint workflows
        run: make actionlint

      - name: Every action is SHA-pinned
        run: make actions-pinned

  helm:
    name: Helm
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: azure/setup-helm@9bc31f4ebc9c6b171d7bfbaa5d006ae7abdb4310 # v5.0.1
        with:
          version: v4.2.3

      - name: Install kubeconform
        run: |
          set -euo pipefail
          curl -sSL -o kubeconform.tar.gz \
            https://github.com/yannh/kubeconform/releases/download/v0.8.0/kubeconform-linux-amd64.tar.gz
          tar xzf kubeconform.tar.gz kubeconform
          sudo install -m 0755 kubeconform /usr/local/bin/kubeconform

      - name: Lint, template permutations, required values, schema
        run: make helm

  docker:
    name: Docker
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install hadolint
        run: |
          set -euo pipefail
          sudo curl -sSL -o /usr/local/bin/hadolint \
            https://github.com/hadolint/hadolint/releases/download/v2.14.0/hadolint-linux-x86_64
          sudo chmod +x /usr/local/bin/hadolint

      - name: Lint the Dockerfile
        run: make docker-lint

      - uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4.2.0

      # Built here rather than via `make docker-build` so the layer cache can be
      # shared across the three builds of the same content: the PR run, the run
      # on main after merge, and the release. `load: true` puts the result in
      # the local daemon so the smoke test below is the same one run locally.
      - name: Build the image
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        with:
          context: .
          load: true
          tags: truenas-scale-exporter:dev
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Smoke test it
        run: make docker-smoke

      - name: Install grype
        id: grype
        uses: anchore/scan-action/download-grype@e1165082ffb1fe366ebaf02d8526e7c4989ea9d2 # v7.4.0
        with:
          # The vulnerability DB is a ~100 MB download on every run, and was
          # measured at 53s of an 87s job — twice the build it guards.
          cache-db: "true"

      # The action only returns an absolute path in its `cmd` output; it does
      # not put grype on PATH. `make scan` invokes grype by name so that the
      # same target works locally, so the directory has to be added here.
      - name: Put grype on PATH
        run: dirname "${{ steps.grype.outputs.cmd }}" >> "$GITHUB_PATH"

      - name: Scan the image
        run: make scan
